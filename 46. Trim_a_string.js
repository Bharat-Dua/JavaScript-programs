//*-------- Trim a string

//? This program trim leading and trailing whitespaces from a string using the trim() method.

function trimString(str) {
  console.log(str.trim());
  return str.trim();
}
trimString("       frontend            developer       ");
